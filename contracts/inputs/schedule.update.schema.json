{
  "title": "schedule.update 입력",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "requirements"
  ],
  "properties": {
    "requirements": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "description": "담당 업무의 목표와 필요한 기준 자료를 포함한 요청. 입력 전제: 기준 일정; 실적 또는 변경 사항"
    },
    "instructions": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "description": "담당 범위 안의 추가 제약 또는 형식 지시"
    },
    "baseline": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "description": "baseline에 해당하는 고정된 기준 자료의 본문. 경로만 적는 경우 실행 전에 접근 가능한 자료로 해석해야 한다."
    },
    "changes": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "description": "changes에 해당하는 고정된 기준 자료의 본문. 경로만 적는 경우 실행 전에 접근 가능한 자료로 해석해야 한다."
    }
  }
}
