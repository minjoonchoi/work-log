{
  "title": "delivery.review 입력",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "requirements"
  ],
  "properties": {
    "requirements": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "description": "담당 업무의 목표와 필요한 기준 자료를 포함한 요청. 입력 전제: 전달 대상 산출물; 수용 조건; 검사·검토 근거"
    },
    "instructions": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "description": "담당 범위 안의 추가 제약 또는 형식 지시"
    },
    "artifacts": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "description": "artifacts에 해당하는 고정된 기준 자료의 본문. 경로만 적는 경우 실행 전에 접근 가능한 자료로 해석해야 한다."
    },
    "acceptance_criteria": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "description": "acceptance_criteria에 해당하는 고정된 기준 자료의 본문. 경로만 적는 경우 실행 전에 접근 가능한 자료로 해석해야 한다."
    },
    "evidence": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "description": "evidence에 해당하는 고정된 기준 자료의 본문. 경로만 적는 경우 실행 전에 접근 가능한 자료로 해석해야 한다."
    }
  }
}
