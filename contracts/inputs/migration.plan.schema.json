{
  "title": "migration.plan 입력",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "requirements"
  ],
  "properties": {
    "requirements": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "description": "담당 업무의 목표와 필요한 기준 자료를 포함한 요청. 입력 전제: 기존·목표 스키마; DB와 데이터·운영 제약"
    },
    "instructions": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "description": "담당 범위 안의 추가 제약 또는 형식 지시"
    },
    "current_schema": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "description": "current_schema에 해당하는 고정된 기준 자료의 본문. 경로만 적는 경우 실행 전에 접근 가능한 자료로 해석해야 한다."
    },
    "target_schema": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "description": "target_schema에 해당하는 고정된 기준 자료의 본문. 경로만 적는 경우 실행 전에 접근 가능한 자료로 해석해야 한다."
    },
    "operating_constraints": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "description": "operating_constraints에 해당하는 고정된 기준 자료의 본문. 경로만 적는 경우 실행 전에 접근 가능한 자료로 해석해야 한다."
    }
  }
}
